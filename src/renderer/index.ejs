<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta
      http-equiv="Content-Security-Policy"
      content="script-src 'self' 'unsafe-inline'"
    />
    <title>IrDA Sim</title>
  </head>
  <body>
    <div id="root"></div>
  </body>
  <script>
    let ports = []

    const ir = window.electron.ipcRenderer
    ir.once('ipc-example', (arg) => {
      console.log(arg);
    });

    setInterval(_=>ir.portsReq(), 1000)

    ir.myPing();

  </script>
</html>
